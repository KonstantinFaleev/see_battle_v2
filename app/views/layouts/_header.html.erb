<nav class="navbar navbar-inverse">
  <div class="container">
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1"><h1>Sea Battle - GeekHub</h1>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Home", root_path %></li>
        <% if signed_in? %>
            <li><%= link_to "Players", players_path, :rel => "external" %></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                My Account <b class="caret"></b>
              </a>
              <ul class="dropdown-menu" role="menu">
                <li>
                  <%= link_to current_player do %>
                      <span class="glyphicon glyphicon-user"></span> Profile
                  <% end %>
                </li>
                <li>
                  <%= link_to edit_player_path(current_player) do %>
                      <span class="glyphicon glyphicon-wrench"></span> Settings
                  <% end %>
                </li>
                <% if current_player.admin? %>
                    <li>
                      <%= link_to comments_path do %>
                          <span class="glyphicon glyphicon-list-alt"></span> Moderate comments
                      <% end %>
                    </li>
                <% end %>
                <li class="divider"></li>
                <li>
                  <%= link_to signout_path, method: "delete" do %>
                      <span class="glyphicon glyphicon-off"></span> Sign out
                  <% end %>
                </li>
              </ul>
            </li>
        <% else %>
            <li><%= link_to "Sign in", signin_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>